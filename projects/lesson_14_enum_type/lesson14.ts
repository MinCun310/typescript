// Enum Types (liệt kê)
enum API_STATUS {
    PENDING = "pending",
    FULFILLED = "fulfilled",
    REJECTED = "rejected"
    //PROMISE
}
let a1 = API_STATUS.PENDING
let a2 = API_STATUS.FULFILLED

console.log("a1 = ", a1, ",a2 = ", a2)